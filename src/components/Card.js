import React from 'react';
import '../styles/card.css';

const Card = ({data}) => {
    return (
        <div className='card' id={data.node.id}>
        
        </div>
    )
};

export default Card;
